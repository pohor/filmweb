<%= form_with(model: screenwriter, local: true) do |form| %>
  <% if screenwriter.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(screenwriter.errors.count, "error") %> prohibited this screenwriter from being saved:</h2>

      <ul>
      <% screenwriter.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
